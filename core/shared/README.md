# Shared

Common data, constants, and enumerations shared across HyperOS applications.

## Contents

- Enum definitions (system states, profile types, etc.)
- Constant values (paths, defaults, limits)
- Data transfer objects
- Shared type definitions

## Design

All shared definitions are versioned and backward compatible.
