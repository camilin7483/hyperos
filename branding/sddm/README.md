# SDDM Theme

HyperOS SDDM login screen theme.

## Status

Placeholder. Final theme will be created during development.

## Files

- `theme.conf` — SDDM theme configuration

## Installation

The theme is installed to `/usr/share/sddm/themes/hyperos/` and configured in `/etc/sddm.conf`.
