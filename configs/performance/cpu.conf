# HyperOS CPU Performance Configuration
# /etc/hyperos/cpu.conf

[governor]
# Available: performance, powersave, schedutil
default = schedutil
battery = powersave
plugged = schedutil

[scaling]
min_freq = 0
max_freq = 0
energy_performance = balance_performance

[boost]
allow_boost = true
