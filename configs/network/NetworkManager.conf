# HyperOS NetworkManager Configuration
# /etc/NetworkManager/conf.d/hyperos.conf

[main]
plugins=keyfile
dhcp=internal

[connectivity]
uri=http://detectportal.firefox.com/success.txt
interval=300

[global-dns]
# DNS over TLS (future)
# domains=

[wifi]
backend=iwd
powersave=2
