# Kernel Scripts

Scripts for kernel management and building.

## Planned Scripts

- `build-kernel.sh` — Build custom linux-hyperos kernel
- `install-kernel.sh` — Install a kernel variant
- `config-kernel.sh` — Configure kernel options

## Status

Placeholder for future kernel development.
